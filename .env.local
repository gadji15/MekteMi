# Frontend (Next.js) - Local example env file

# Base URL of the Laravel API (local dev)
# If you use Laravel Sail (nginx on port 80):
NEXT_PUBLIC_API_BASE_URL=http://localhost
NEXT_PUBLIC_API_TIMEOUT=10000
# If you use "php artisan serve", uncomment the next line instead:
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
